erro_1:
a) função chamada com parâmetros inválidos (número e tipo); a função g foi chamada na linha 17 com passagem de parâmetros, porém, a função não exige parâmetros.
b) função chamada com parâmetros inválidos (tipo); a função f foi chamada na linha 36 com argumentos do tipo graph, porém, f deve receber um parâmetro do tipo int.
c) função chamada com parâmetro inválidos (número e tipo); a função f foi chamada na linha 37 sem argumentos, porém, f deve receber um parâmetro do tipo int.

erro_2:
a) operador usado com tipo inválido; o operador unário '-' (para numeros) foi aplicado ao tipo grafo na linha 23.
b) opeardor usado com tipo inválido; o operador unário '!' (para boleanos) foi aplicado ao tipo grafo na linha 24.
c) comando if aplicado a tipo inválido; o if da linha 26 recebeu como argumento uma chamada à função g (int), em vez de uma expressão booleana.
d) comando while aplicado a tipo inválido; o while da linha 33 recebeu como argumento uma variável do tipo graph, em vez de uma expressão booleana.
